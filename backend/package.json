{
  "name": "backend",
  "version": "1.0.0",
  "description": "API de Nous",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "migrate": "node-pg-migrate --migrations-dir src/infrastructure/db/migrations --migration-file-language ts",
    "migrate:up": "node-pg-migrate up --migrations-dir src/infrastructure/db/migrations --migration-file-language ts",
    "migrate:down": "node-pg-migrate down --migrations-dir src/infrastructure/db/migrations --migration-file-language ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:migrate": "node-pg-migrate up --migrations-dir src/infrastructure/db/migrations --migration-file-language ts -d TEST_DATABASE_URL"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@fastify/helmet": "^13.1.0",
    "@fastify/rate-limit": "^11.2.0",
    "dotenv": "^17.4.2",
    "fastify": "^5.12.1",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/pg": "^8.20.4",
    "node-pg-migrate": "^9.0.0",
    "tsx": "^4.23.10",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
